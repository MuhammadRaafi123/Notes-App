import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  welcome: {
    color: '#0F172A',
    fontSize: 26,
    fontWeight: '800',
    textAlign: 'center',
  },
  subtitle: {
    color: '#64748B',
    textAlign: 'center',
    marginTop: 6,
    marginBottom: 24,
    fontSize: 14,
    lineHeight: 20,
  },
  register: {
    marginTop: 20,
    textAlign: 'center',
    color: '#5B8EA6',
    fontWeight: '600',
    fontSize: 14,
  },
});

export default styles;
